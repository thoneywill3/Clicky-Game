import React from "react";

const Header = () => (
  <div className="jumbotron p-3">
    <h1 className="text-center">Click the Star Wars Characters!</h1>
    <h3 className="text-center">Click on an image below to earn points, but don't click on any more than once to keep playing!</h3>
  </div>

)
export default Header;